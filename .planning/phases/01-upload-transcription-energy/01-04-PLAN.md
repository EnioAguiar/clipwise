---
phase: 01-upload-transcription-energy
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/UploadTab.tsx
  - frontend/src/components/ConfigPanel.tsx
  - frontend/src/app/page.tsx
autonomous: true
requirements:
  - CONF-01
  - CONF-02
  - CONF-03
  - CONF-04
  - CONF-05
---

<objective>
Build the configuration UI panel for clip settings: min/max duration, target clip count, output format, and auto/manual mode selection.
</objective>

<context>
@.planning/phases/01-upload-transcription-energy/01-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@frontend/src/app/page.tsx
@frontend/src/components/
</context>

<interfaces>
<!-- Key UI decisions from CONTEXT.md -->

D-07: Manual button "Transcribe" — user clicks after upload/URL entry
D-09: Configuration UI appears AFTER processing (not before)
D-10: Everything on same page, progress bar with % + step list
D-11: Log window showing each step as it happens

CONF requirements:
- CONF-01: min clip duration (20s to 3min, default 30s)
- CONF-02: max clip duration (30s to 5min, default 60s)
- CONF-03: target number of clips (3-20, default 10)
- CONF-04: output format (vertical 9:16 or square 1:1)
- CONF-05: mode (auto or manual)
</interfaces>

<tasks>

<task type="auto">
  <name>task 1: Create ConfigPanel component with all settings</name>
  <files>frontend/src/components/ConfigPanel.tsx</files>
  <read_first>frontend/src/app/page.tsx</read_first>
  <action>
Create `frontend/src/components/ConfigPanel.tsx` with:

1. **Props interface:**
   ```typescript
   interface ConfigPanelProps {
     config: ProcessingConfig;
     onConfigChange: (config: ProcessingConfig) => void;
     disabled?: boolean;
   }

   interface ProcessingConfig {
     minDuration: number;    // seconds, range 20-180, default 30
     maxDuration: number;    // seconds, range 30-300, default 60
     targetClips: number;   // range 3-20, default 10
     format: "vertical" | "square";  // 9:16 or 1:1
     mode: "auto" | "manual";        // auto = top moments, manual = user reviews
   }
   ```

2. **UI Controls:**
   - Min duration: slider 20-180s with numeric display (default 30)
   - Max duration: slider 30-300s with numeric display (default 60)
   - Target clips: slider 3-20 with numeric display (default 10)
   - Format: toggle between "Vertical (9:16)" and "Square (1:1)" (default vertical)
   - Mode: toggle between "Auto" and "Manual" (default auto)
   - Each control shows current value clearly

3. **Styling:** Use Tailwind + shadcn/ui components. Clean, minimal layout.

4. **Constraints enforced in UI:**
   - minDuration < maxDuration always (min can't exceed max)
   - Sliders have proper range limits per requirements
</action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | head -30</automated>
  </verify>
  <done>
ConfigPanel.tsx exists with all 5 configuration controls
Min/max duration sliders with proper ranges
Target clips slider (3-20, default 10)
Format toggle (vertical/square)
Mode toggle (auto/manual)
ConfigPanel props interface defined
</done>
</task>

<task type="auto">
  <name>task 2: Update main page to show ConfigPanel after processing</name>
  <files>frontend/src/app/page.tsx</files>
  <read_first>frontend/src/app/page.tsx</read_first>
  <action>
Update `frontend/src/app/page.tsx` to:

1. **Tabs at top:** "Arquivo" | "YouTube" — single toggle box (D-02)

2. **Upload/URL input section** with dropzone for file or text input for YouTube URL

3. **Transcribe button** — manual button click (D-07), NOT automatic

4. **Progress display** (D-10): Show both progress bar with % AND step list simultaneously:
   - Step list shows: "Downloading YouTube... 45%", "Transcribing... 67%", "Extracting energy... 89%"
   - Progress bar fills as steps complete

5. **Log window** (D-11): Real-time log showing each step as it happens
   - If error: shows what happened with retry option
   - NOT toast notification — actual log window

6. **ConfigPanel** appears AFTER processing completes (D-09)
   - Same page, below processing section
   - Shows current config values

7. **Processing state management:**
   - States: idle, uploading, downloading, transcribing, analyzing, complete, error
   - Current step + percentage displayed

Follow shadcn/ui patterns for components. Use Tailwind for layout.
</action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | head -30</automated>
  </verify>
  <done>
Page.tsx has Arquivo/YouTube tabs
Transcribe button triggers processing manually
Progress bar + step list shown during processing
Log window displays real-time step updates
ConfigPanel appears after processing completes
</done>
</task>

<task type="auto">
  <name>task 3: Connect ConfigPanel to backend processing state</name>
  <files>frontend/src/app/page.tsx</files>
  <read_first>frontend/src/app/page.tsx</read_first>
  <action>
Update page.tsx to wire up ConfigPanel with backend:

1. **API calls to backend:**
   - POST /upload — file upload
   - POST /youtube — YouTube URL download
   - POST /transcribe — triggers transcription (calls transcription.py)
   - POST /energy — triggers energy extraction
   - GET /video/{file_id}/info — get metadata

2. **Config persistence (CONF-05):**
   - Save config to localStorage when changed
   - Load from localStorage on mount
   - Include config in API requests

3. **Frontend-backend integration:**
   - Upload file → get file_id
   - file_id used for subsequent /transcribe and /energy calls
   - Results displayed in UI

4. **Error handling:**
   - Show error in log window with retry button
   - Errors include step name and message from backend
</action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | head -30</automated>
  </verify>
  <done>
Config changes persist to localStorage
API calls wired: upload → transcribe → energy flow
Error display with retry in log window
</done>
</task>

</tasks>

<verification>
1. Start frontend: `cd frontend && npm run dev`
2. Start backend: `cd backend && source venv/bin/activate && uvicorn main:app --port 8000`
3. Navigate to http://localhost:3000
4. Upload a test file — should show progress
5. Verify ConfigPanel appears after processing with all settings
6. Change settings — verify they persist on refresh
</verification>

<success_criteria>
- All 5 CONF requirements covered (CONF-01 through CONF-05)
- Min/max duration sliders with proper ranges (CONF-01, CONF-02)
- Target clips slider 3-20 (CONF-03)
- Format toggle (vertical/square) (CONF-04)
- Mode toggle (auto/manual) (CONF-05)
- Config persists to localStorage
- All on same page (D-10), config appears after processing (D-09)
</success_criteria>

<output>
After completion, create `.planning/phases/01-upload-transcription-energy/01-04-SUMMARY.md`
</output>